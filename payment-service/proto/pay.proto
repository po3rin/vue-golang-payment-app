syntax = "proto3";

package paymentservice;

// For grpc gateway
// import "google/api/annotations.proto";


service PayManager {
  rpc Charge (PayRequest) returns (PayResponse) {}
}

message PayRequest {
  int64 amount = 1;
  string num = 2;
  string cvc = 3;
  string expm = 4;
  string expy = 5;
}

message PayResponse {
  bool paid = 1;
  bool captured = 3;
  int64 amount = 2;
}
